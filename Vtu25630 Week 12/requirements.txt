# requirements.txt — Python dependencies
# Session 57–60: Used in CI pipeline (pip install -r requirements.txt)

# Web framework
Flask==3.0.3

# Testing
pytest==8.2.2
pytest-cov==5.0.0       # Coverage reports (Session 60)

# Linting (installed separately in CI, listed here for local dev)
flake8==7.1.0
